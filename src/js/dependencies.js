//load dependencies for bundling
//for now app code is in ./app, without compilation step required

var $ = window.$ = window.jQuery = require('jquery');
var _ = window._ = require('underscore');

require('bootstrap');

require('leaflet');

// require('leaflet-markercluster'); //nope :(
